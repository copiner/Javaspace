#include<iostream>
#include<vector>
using namespace std;

int main(){
  vector< vector<int> > b(10,vector<int>(5,1));

  for(int i=0;i<b.size();i++){
    for(j=0;j<b[i].size();j++){
      cout<<b[i][j]<<" ";
    }
    cout<<"\n";
  }
}


TCP(Transmission Control  Protocol)
TCP客户与服务器之间的链接(connection)。TCP客户先与某个给定的服务器建立一个链接，再跨该链接与那个服务器交换数据，然后中止这个链接。当TCP向另一端发送数据时，它要求对端返回一个确认。如果没有收到确认，TCP就自动重传数据并等待更长时间。在数次重传失败后，TCP才放弃，如此尝试发送数据上所花费的总时间一般为4~10分钟（依赖具体实现）。

TCP含有用于动态估算客户与服务器之间的往返时间(round-trip time, RTT)的算法，以便知道等待一个确认需要多长时间。